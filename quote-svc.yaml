apiVersion: v1
kind: Service
metadata:
  labels:
    run: quote-service-db
  name: quote-service-db
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    run: quote-service-db
  sessionAffinity: None
  type: ClusterIP
